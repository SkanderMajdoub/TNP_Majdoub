import { RealisateurPipe } from './realisateur.pipe';

describe('RealisateurPipe', () => {
  it('create an instance', () => {
    const pipe = new RealisateurPipe();
    expect(pipe).toBeTruthy();
  });
});
